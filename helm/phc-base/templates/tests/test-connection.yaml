apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phc-base.fullname" . }}-test-connection"
  labels:
    {{- include "phc-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phc-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
